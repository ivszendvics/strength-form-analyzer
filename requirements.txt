# Core runtime dependencies. See pyproject.toml for the packaged version.
# mediapipe pinned <1.0: see pyproject.toml for why (1.0.1 crashes on some
# macOS setups with the PoseLandmarker graph).
mediapipe>=0.10.18,<0.11
opencv-python>=4.9.0
numpy>=1.26,<2.0
scipy>=1.11
matplotlib>=3.8
pyyaml>=6.0
pandas>=2.1

# Development / testing
pytest>=8.0
pytest-cov>=5.0
