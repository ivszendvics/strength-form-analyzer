# Exact, fully reproducible dependency set for this project, generated with
# `pip freeze` from a clean install of requirements.txt (Python 3.11.9) and
# verified against the full test suite (`pytest`) plus a live MediaPipe
# pose-estimation smoke test.
#
# requirements.txt / pyproject.toml define the *intended* version ranges;
# this file pins the *exact* versions that were actually tested together.
# Ranges alone can silently drift onto an untested release over time (this
# project hit that directly: mediapipe>=0.10.18,<0.11 resolved to 0.10.35
# at one point, which removed the `mp.solutions` API a fresh install had
# been relying on -- see the git history for src/pose/mediapipe_estimator.py).
#
# Usage:
#   pip install -r requirements-lock.txt
#
# To regenerate after intentionally updating a dependency:
#   python3 -m venv .venv-check && source .venv-check/bin/activate
#   pip install -r requirements.txt && pytest
#   pip freeze --exclude-editable > requirements-lock.txt
#   deactivate && rm -rf .venv-check
# (starting from a completely fresh venv, and not upgrading pip itself
# in it, avoids pip incidentally pulling in its own newer bootstrap
# dependencies as if they were project packages.)

absl-py==2.5.0
certifi==2026.7.22
cffi==2.1.1
contourpy==1.3.3
coverage==7.16.0
cycler==0.12.1
flatbuffers==25.12.19
fonttools==4.64.0
iniconfig==2.3.0
kiwisolver==1.5.1
matplotlib==3.11.1
mediapipe==0.10.35
numpy==1.26.4
opencv-contrib-python==4.11.0.86
opencv-python==4.11.0.86
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pluggy==1.6.0
pycparser==3.0
Pygments==2.21.0
pyparsing==3.3.2
pytest==9.1.1
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
PyYAML==6.0.3
scipy==1.17.1
six==1.17.0
sounddevice==0.5.6
